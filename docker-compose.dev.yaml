# Development compose file - uses vLLM built from source
# Usage:
#   1. Build first: ./run.sh build [branch]
#   2. Run: ./run.sh {profile} up --dev

services:
  vllm:
    extends:
      file: docker-compose.yaml
      service: vllm
    image: vllm-dev:${VLLM_DEV_TAG:-${VLLM_BRANCH:-main}}
