# Profile: NewModel
# GPU: 0, Port: 8002, Model: new-model-name

CONTAINER_NAME=newmodel
VLLM_PORT=8002
CONFIG_NAME=new-model-config

# GPU Configuration
GPU_ID=0
TENSOR_PARALLEL_SIZE=1

# Extra Pip Packages (optional)
# Install additional pip packages before starting vllm serve
# Note: Use archive URL (not git+https) since container doesn't have git
#EXTRA_PIP_PACKAGES=https://github.com/huggingface/transformers/archive/main.zip

# LoRA Configuration (optional)
ENABLE_LORA=false
#MAX_LORAS=2
#MAX_LORA_RANK=16
#LORA_MODULES=adapter1=/app/lora/path1